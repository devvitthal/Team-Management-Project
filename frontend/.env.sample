# Frontend environment variables — copy this file to .env and fill in real values.
# Do NOT commit .env.

# URL of the API gateway (all backend traffic is routed through it)
VITE_API_GATEWAY_URL=http://localhost:8000
